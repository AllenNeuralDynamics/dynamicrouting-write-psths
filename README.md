currently writes psths for boths target in each context (hits and correct rejects for both)

should also do nontarget FAs and correct rejects

TODO
consider storing trial columns (is_target, is_aud_context, etc.) instead of stim+context modality codes (VA, AA) which will get confusing with more trial types